name: ssh-separator
version: 0.1.0
description: SSH connection separator / multiplexer (Go server). Compile-time DI via mazrean/kessoku.

dependencies:
  apm:
    - mazrean/apm-plackage/common
    - mazrean/apm-plackage/go
